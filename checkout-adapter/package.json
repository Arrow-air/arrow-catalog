{
  "name": "arrow-checkout-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Reference manufacturer checkout endpoint for the Arrow checkout handoff convention. Zero runtime dependencies; deploy with your own Stripe key.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
